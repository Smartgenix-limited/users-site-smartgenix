<div class="card-header text-center">
    <h4 class="pt-1 d-inline-flex mb-0 pb-0">{{ $title }}</h4>
    <a href="{{ $url }}" class="btn btn-sm btn-genix float-right">{{ __('general.create') }}</a>
</div>
